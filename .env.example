# Sample environment variables for ubicaTEC
# Copy this file to .env and set real values (do NOT commit .env)

# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=ubicaTEC
DB_PORT=3306

# Email (Gmail recommended: create an App Password and put it here)
EMAIL_USER=ubicatecoficial@gmail.com
EMAIL_PASS=your_app_password_here

# Server
PORT=3000
